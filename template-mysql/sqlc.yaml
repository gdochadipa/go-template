version: "2"
sql:
  - engine: "mysql"
    queries: "db/query/"
    schema: "db/migration/"
    gen:
      go:
        package: "repository"
        out: "internal/user/sqlc"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
