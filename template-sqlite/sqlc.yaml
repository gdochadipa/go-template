version: "2"
sql:
  - engine: "sqlite"
    queries: "db/query/"
    schema: "db/migration/"
    gen:
      go:
        package: "repository"
        out: "internal/adapter/repository/sqlite/sqlc"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
